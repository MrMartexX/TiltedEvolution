name: Equal party PoC diagnostics

on:
  pull_request:
    branches:
      - dev
    paths:
      - "Code/encoding/Structs/Skyrim/QuestSnapshot.*"
      - "Code/tests/quest_snapshot.cpp"
      - ".github/workflows/equal-party-poc-diagnostics.yml"
  workflow_dispatch:

jobs:
  build-poc-tests:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Checkout submodules
        run: |
          git submodule sync --recursive
          git submodule update --init --force --recursive --depth=1

      - name: Setup xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: "3.0.9"

      - name: Configure
        run: |
          xmake repo --update
          xmake config --arch=x64 --mode=debug --yes

      - name: Build TPTests and capture diagnostics
        id: build
        shell: bash
        run: |
          set +e
          set -o pipefail
          xmake build TPTests -vD 2>&1 | tee poc-build.log
          status=${PIPESTATUS[0]}
          grep -Ei "error:|fatal error:|undefined reference|collect2:|error C[0-9]+|LNK[0-9]+" poc-build.log | tail -n 250 > poc-errors.txt || true
          if [ ! -s poc-errors.txt ]; then
            tail -n 300 poc-build.log > poc-errors.txt
          fi
          exit "$status"

      - name: Upload diagnostic log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: equal-party-poc-diagnostics
          path: |
            poc-errors.txt
            poc-build.log
          retention-days: 7
